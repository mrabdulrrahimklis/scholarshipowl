# SQLite database file (Prisma). Relative paths resolve to the prisma/ dir.
DATABASE_URL=file:./dev.db
PORT=4000
# Comma-separated allowed CORS origins for the Nuxt dev server
CORS_ORIGIN=http://localhost:3000
